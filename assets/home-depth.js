(()=>{if(!document.body.classList.contains('ms-home'))return;const motion=matchMedia('(prefers-reduced-motion: reduce)'),fine=matchMedia('(hover: hover) and (pointer: fine)');document.querySelectorAll('.ms-service-card').forEach(card=>{card.addEventListener('pointermove',e=>{if(motion.matches||!fine.matches||e.pointerType!=='mouse')return;const r=card.getBoundingClientRect(),x=(e.clientX-r.left)/r.width,y=(e.clientY-r.top)/r.height;card.style.setProperty('--depth-x',(0.5-y)*5+'deg');card.style.setProperty('--depth-y',(x-0.5)*6+'deg');card.style.setProperty('--light-x',x*100+'%');card.style.setProperty('--light-y',y*100+'%')});card.addEventListener('pointerleave',()=>{card.style.removeProperty('--depth-x');card.style.removeProperty('--depth-y')})});if('IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(!entry.isIntersecting)return;if(!motion.matches)entry.target.classList.add('depth-enter');observer.unobserve(entry.target)}),{threshold:.12});document.querySelectorAll('.ms-heading-row,.ms-about-image,.ms-dashboard-scene,.ms-recruit-visual,.ms-process,.ms-contact-card').forEach(el=>observer.observe(el));}})();
